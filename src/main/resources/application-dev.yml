## EC2 server ##
spring:
    jpa:
      database: mysql
      database-platform: org.hibernate.dialect.MySQLDialect
      hibernate:
        ddl-auto: none
    datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://43.201.71.50:3306/keyDeuk_shop?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
      username: ${EC2_DB_USERNAME}
      password: ${EC2_DB_PASSWORD}

